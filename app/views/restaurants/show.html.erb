<h1><%= @restaurant.name %> </h1>
<p><%= @restaurant.description %></p>
<%= link_to "Back to list", restaurants_path, class: "btn btn-sm btn-primary" %>
<%= link_to 'Edit', edit_restaurant_path(@restaurant), class: "btn btn-sm btn-primary"%>
<%= link_to "Delete", @restaurant, method: :delete,
                              data: { confirm: "#{@restaurant.name} will be permanently deleted. Are you sure you would like to proceed?" } %>
